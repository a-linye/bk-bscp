# 需求评估报告：S-001 #135562734 App 服务完整改造（第 4 轮澄清）

## 基本信息

| 字段 | 值 |
|------|-----|
| 父需求 ID | 1120451610135156014 (#135156014) |
| 子需求 TAPD ID | [1120451610135562734](https://tapd.tencent.com/tapd_fe/20451610/story/detail/1120451610135562734) |
| 评估时间 | 2026-06-26（第 4 轮更新） |
| 评估人 | AI Agent (CodeBuddy) |

---

## 一、核心约束（第 4 轮澄清确认）

> **用户原话**（2026-06-26 17:03）：
> 1. **只有 App 表存在 project_id 和 environment_id**
> 2. **KV 和文件配置只需要检查是否属于该业务+项目+环境**（通过 App 关联，不单独加字段）
> 3. **旧路由保持原来的默认注入**（硬编码默认项目和默认环境的 fallback）
> 4. **新路由会带上项目和环境**
> 5. **本期只针对 App 服务完整改造**
> 6. **每个新旧路由都需要调试测试**

---

## 二、代码现状精确盘点（逐方法）

### 2.1 已完成的改造（无需额外开发，仅需验证测试）

#### 数据模型层 — ✅ 全部完成

| 文件 | 状态 | 说明 |
|------|------|------|
| `pkg/dal/table/app.go` | ✅ | ProjID (L34) + EnvID (L35) 字段已添加 |
| `internal/dal/dao/app.go` | ✅ | 所有方法签名已携带 projectID/envID 参数（Get/List/Create/Delete/GetByName/GetByAlias/CountApps 等）|
| `pkg/protocol/core/app/app.proto` | ✅ | App message 已有 project_id=9, env_id=10 |
| `pkg/kit/kit.go` | ✅ | ResolvedProjectID / ResolvedEnvID 解析逻辑完成 |

#### Proto Request Message — ✅ 全部完成

所有 App 相关 Request Message 均已有 `project_id` 和 `env_id` 字段：

| Message | config_server.proto | data_service.proto |
|---------|:-:|:-:|
| CreateAppReq | ✅ field=10,11 | ✅ field=3,4 |
| UpdateAppReq | ✅ field=10,11 | ✅ field=4,5 |
| DeleteAppReq | ✅ field=3,4 | ✅ field=3,4 |
| GetAppReq | ✅ field=3,4 | ✅ field=3,4 |
| GetAppByNameReq | ✅ field=3,4 | ✅ field=3,4 |
| ListAppsRestReq | ✅ field=6,7 | ✅ field=8,9 |
| ListAppsBySpaceRestReq | ✅ field=8,9 | — |
| CloneAppReq | ✅ field=16,17 | ✅ field=16,17 |

#### ConfigServer App 方法 — ✅ 8/8 方法已完成适配

| 方法名 | 行号 | 维度字段传递 | 状态 |
|--------|------|-------------|------|
| **CreateApp** | L69-70 | `kt.ResolvedProjectID(req.GetProjectId())`, `kt.ResolvedEnvID(req.GetEnvId())` | ✅ |
| **UpdateApp** | L117-118 | 同上 | ✅ |
| **DeleteApp** | L146-147 | 同上 | ✅ |
| **GetApp** | L173-174 | 同上 | ✅ |
| **GetAppByName** | L192-193 | 同上 | ✅ |
| **ListAppsBySpaceRest** | L298-299 | 同上 | ✅ |
| **CloneApp** | L485-486 | 同上 | ✅ |
| **ListAppsRest** | L241-246 | ⚠️ 未传 BizId/ProjectId/EnvId（旧路由，TODO 注释）| ⚠️ 保留原行为 |

#### DataService App 方法 — ✅ 8/8 方法已完成适配

| 方法名 | 行号 | 维度处理方式 | 状态 |
|--------|------|-------------|------|
| **CreateApp** | L48-49, L74-75 | 读取 req.ProjectId/EnvId → 写入 table.App.ProjID/EnvID | ✅ |
| **UpdateApp** | L100-101, L131-132 | 读取 → Get 校验(4维) → 更新时携带维度 | ✅ |
| **DeleteApp** | L195-198 | 读取 → Get 校验(4维)验证归属 | ✅ |
| **GetApp** | L332 | 直接传 4 参数到 DAO.Get() | ✅ |
| **GetAppByName** | L361 | 直接传 4 参数到 DAO.GetByName() | ✅ |
| **ListAppsRest** | L381-382, L397, L403 | 读取 → DAO.List(4参数) → DAO.CountApps(4参数) | ✅ |
| **CloneApp** | L534-535 | 读取 req → 写入 table.App.ProjID/EnvID | ✅ |
| **GetAppByID** | L345 | 仅按 ID 查（内部调用用，无需维度过滤）| ✅ 设计如此 |

### 2.2 新增的中间件（上一轮已完成）

| 改动 | 文件 | 行号 | 状态 |
|------|------|------|------|
| **AppProjectEnvVerified 中间件** | `cmd/api-server/service/middleware.go` | L137-170 | ✅ 已实现 |
| **中间件注册到路由** | `cmd/api-server/service/routers.go` | L85 | ✅ `{app_id}` 路由组已注册 |

### 2.3 待完成的工作项

---

## 三、待完成工作项详细清单与工时估算

### 工作总览

```
┌─────────────────────────────────────────────────────────────┐
│                  本期范围：仅 App 服务                        │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  已完成（代码层面）✅                                        │
│  ├── App 数据模型 + DAO + Proto                    无需改动 │
│  ├── ConfigServer 8 个 App 方法全部已适配              无需改动 │
│  └── DataService 8 个 App 方法全部已适配              无需改动 │
│                                                             │
│  待完成 🔧                                                   │
│  ├── W-A: 清理 data-service 冗余校验代码 (28处)              │
│  ├── W-B: 中间件默认值机制完善（硬编码→真实查询 或 配置化）    │
│  ├── W-C: 逐个接口新旧路由联调测试                            │
│  └── W-D: 边界场景与兼容性测试                               │
└─────────────────────────────────────────────────────────────┘
```

### W-A：清理 data-service 冗余校验代码（~2h 开发 + 1h 测试 = 3h）

**背景**：api-server 的 `AppProjectEnvVerified` 中间件已统一拦截校验，data-service 内部 28 处重复的 `s.dao.App().Get()` 归属校验成为冗余代码。

**涉及文件和位置**：

| 文件 | 删除数量 | 说明 |
|------|---------|------|
| `cmd/data-service/service/config_item.go` | **10 处** | BatchUpsert/BatchUpdate/Create/Update/Delete/Publish 等 |
| `cmd/data-service/service/kv.go` | **8 处** | Update/List/Delete/UnDelete/Undo/FetchIDs/FetchKeys/FindNearExpiryCert |
| `cmd/data-service/service/release.go` | **6 处** | CreateRelease/ListReleases/GetReleaseByName/GetRelease/Deprecate/UnDeprecate |
| `cmd/data-service/service/released_kv.go` | **2 处** | GetReleasedKv/ListReleasedKvs |
| `cmd/data-service/service/released_ci.go` | **2 处** | GetReleasedConfigItem/ListReleasedConfigItems |
| **合计** | **28 处** | |

**注意**：
- `cmd/data-service/service/app.go` 中的 `Get` (L332) **不删除**——这是业务逻辑本身，返回值 `app` 被实际使用
- `DeleteApp` 中的 `Get` (L198) **不删除**——返回值 `app` 用于后续 `DeleteWithTx`
- 每处删除后需要编译验证 + 跑单包测试

**预估时间**：开发 2h + 编译验证 0.5h + 单包测试 0.5h = **3h**

---

### W-B：中间件默认值回退机制完善（~4h 开发 + 2h 测试 = 6h）

**现状**：`middleware.go:L126-127` 硬编码为 `(7, 7)`

```go
// 当前实现（硬编码）
kt.ProjectID = 7
kt.EnvID = 7
```

**目标方案（二选一，建议方案 B）：

| 方案 | 做法 | 优劣 | 工时 |
|------|------|------|------|
| **A: 真实 RPC 查询** | 调用 `cfgClient.EnsureDefaultProjectEnv` 获取真实默认值 | 准确但依赖新 RPC 接口 | ~6h |
| **B: 配置化默认值** | 从 `cc.ApiServer().DefaultProjectID / DefaultEnvID` 读取，不再硬编码 | 简单可控，后续再接 RPC | **~4h（推荐）** |

**推荐方案 B 实施步骤**：
1. 在 `pkg/cc/types.go` 的 ApiServer 结构体新增 `DefaultProjectID` / `DefaultEnvID` 字段
2. 在 `etc/api-server/*.yml` 添加配置项
3. 修改 `middleware.go` 从 `cc.ApiServer()` 读取替代硬编码
4. 编写单元测试验证中间件行为

**预估时间**：开发 4h + 测试 2h = **6h**

---

### W-C：逐个 App 接口新旧路由联调测试（~16h，核心工作）

**用户明确要求**："我需要每个新旧路由去调试"

#### App 服务接口路由映射表

| 序号 | 操作 | 旧路由（无 project/env）| 新路由（带 project/env）| CS 方法 | DS 方法 | 测试重点 |
|------|------|---------------------|----------------------|---------|---------|---------|
| C-01 | **创建 App** | POST `/config/biz/{biz_id}/apps` | 同左（body 带 project_id/env_id）| CreateApp | CreateApp | 旧路由→默认(7,7)；新路由→携带值写入 DB |
| C-02 | **查询 App 详情** | GET `/config/biz/{biz_id}/apps/{app_id}` | 同左（query 带 project_id/env_id）| GetApp | GetApp | 旧路由→查默认项目；新路由→查指定项目；跨项目返回 not found |
| C-03 | **按名称查询** | GET `/config/biz/{biz_id}/apps/name/{name}` | 同左（带 project_id/env_id）| GetAppByName | GetAppByName | 同 C-02，验证 name 在指定 project+env 下唯一性 |
| C-04 | **更新 App** | PUT `/config/biz/{biz_id}/apps/{app_id}` | 同左（body 带 project_id/env_id）| UpdateApp | UpdateApp | 验证更新后 ProjID/EnvID 不变（不可跨项目迁移）|
| C-05 | **删除 App** | DELETE `/config/biz/{biz_id}/apps/{app_id}` | 同左（带 project_id/env_id）| DeleteApp | DeleteApp | 验证只能删除当前项目下的 App |
| C-06 | **分页列表（旧）**| GET `/config/biz/{biz_id}/apps/query` | — | ListAppsRest | ListAppsRest | 旧路由→走 checkOrCreateDefaultProjectEnv→默认(7,7)→只列默认项目的 App |
| C-07 | **分页列表（新）**| — | GET `/config/biz/{biz_id}/apps/{app_id}?list=...` | ListAppsBySpaceRest | ListAppsRest | 新路由→携带 project_id/env_id→只列该项目的 App |
| C-08 | **克隆 App** | POST `/config/biz/{biz_id}/apps/{app_id}/clone` | 同左（body 带 project_id/env_id）| CloneApp | CloneApp | 克隆后的 App 继承源 App 的 ProjID/EnvID |
| C-09 | **GetAppByID（内部）**| 不经过 api-server | gRPC 直连 | — | GetAppByID | 仅按 ID 查（cache-service/feed-service 等内部调用），不受影响 |

#### 每个接口的测试矩阵

```
测试维度：
┌────────────────────────────────────────────────────────┐
│  接口 X (如 CreateApp)                                │
│                                                        │
│  场景 1: 旧路由（不带 project_id/env_id）               │
│    → 期望: 自动使用默认项目(7)+默认环境(7)             │
│    → 验证: DB 中 ProjID=7, EnvID=7                   │
│                                                        │
│  场景 2: 新路由（携带有效 project_id=1, env_id=2）     │
│    → 期望: 使用请求中的值                              │
│    → 验证: DB 中 ProjID=1, EnvID=2                   │
│                                                        │
│  场景 3: 新路由（携带无效 project_id）                  │
│    → 期望: 返回错误                                    │
│    → 验证: 错误码正确                                  │
│                                                        │
│  场景 4: 新路由（project_id 有效但 env_id 不匹配）       │
│    → 期望: 返回 "环境不属于该项目"                       │
│    → 验证: AppProjectEnvVerified 中间件拦截              │
│                                                        │
│  场景 5: 跨项目访问                                     │
│    → 期望: A 项目用户无法查看 B 项目 App                │
│    → 验证: Get 返回 not found                          │
│                                                        │
│  场景 6: 兼容性（旧客户端不传新字段）                    │
│    → 期望: 正常运行，使用默认值                         │
│    → 验证: 与场景 1 结果一致                           │
└────────────────────────────────────────────────────────┘
```

**预估时间**：
- 9 个接口 × 每接口 6 个场景 × 平均 20 分钟/场景 = **18h**
- 含环境准备、数据清理、问题修复时间
- 合计 **~16h**

---

### W-D：边界场景与回归测试（~4h）

| 测试类别 | 场景数 | 说明 | 时间 |
|----------|-------|------|------|
| 并发安全 | 3 | 同一 App 并发 Update/Delete；新旧路由同时操作同一 App | 1h |
| 数据一致性 | 3 | 创建后立即查询；更新后跨路由查询；删除后验证关联 KV/CI 也被清理 | 1h |
| 权限边界 | 2 | 非 AppOwner 用户能否通过新路由访问；IAM 鉴权链路完整性 | 1h |
| 性能基准 | 2 | AppProjectEnvVerified 中间件增加一次 RPC 调用的延迟影响；对比新旧路由响应时间 | 1h |
| **合计** | **10 场景** | | **4h** |

---

## 四、工时汇总

| 工作项 | 内容 | 开发(h) | 测试(h) | 小计 |
|--------|------|---------|---------|------|
| **W-A** | 清理 data-service 28 处冗余校验代码 | 2 | 1 | **3h** |
| **W-B** | 中间件默认值回退机制完善（去硬编码）| 4 | 2 | **6h** |
| **W-C** | 9 个 App 接口 × 6 场景联调测试 | — | 16 | **16h** |
| **W-D** | 边界场景与回归测试 | — | 4 | **4h** |
| **Buffer** | 问题修复、环境重置、意外排查 | — | 3 | **3h** |
| **总计** | | **6h** | **26h** | **32h（4 人天）** |

---

## 五、实施计划

### Day 1：代码清理 + 中间件完善

```
上午 (4h):
  [W-A] 清理 data-service 28 处冗余校验
    ├── config_item.go  10 处 (~1h)
    ├── kv.go           8 处 (~0.8h)
    ├── release.go      6 处 (~0.6h)
    ├── released_kv.go  2 处 (~0.2h)
    ├── released_ci.go  2 处 (~0.2h)
    └── go build + 单包 test (~0.2h)

下午 (4h):
  [W-B] 中间件默认值机制完善
    ├── cc 类型定义 + yaml 配置 (~1h)
    ├── middleware.go 替换硬编码 (~0.5h)
    ├── 编译验证 (~0.5h)
    └── 单元测试编写 (~2h)
```

### Day 2-3：接口联调（核心）

```
Day 2 上午: C-01 CreateApp (新旧路由 × 6 场景)  ~2h
Day 2 下午: C-02 GetApp + C-03 GetAppByName        ~3h
Day 3 上午: C-04 UpdateApp + C-05 DeleteApp         ~3h
Day 3 下午: C-06 ListAppsRest(旧) + C-07 List(新)  ~3h
```

### Day 4：收尾 + 回归

```
Day 4 上午: C-08 CloneApp                             ~2h
Day 4 下午: [W-D] 边界场景 + 回归测试                 ~4h
          Buffer 时间用于问题修复                      ~2h
```

---

## 六、风险提示

| 风险 | 概率 | 影响 | 缓解措施 |
|------|------|------|---------|
| 清理 28 处遗漏导致编译失败 | 低 | 中 | 每次 replace 后立即 `go build` 验证 |
| 默认值(7,7)在测试环境不存在对应记录 | 中 | 高 | 先确认测试环境是否有 project_id=7, env_id=7；若无则先插入或改配置 |
| AppProjectEnvVerified 增加 ~50ms 延迟 | 低 | 低 | 后续可考虑本地缓存 App 归属关系 |
| 旧前端/SDK 不传新字段导致行为不一致 | 中 | 中 | 旧路由必须严格走默认值路径，不做任何破坏性变更 |
| CloneApp 跨项目克隆逻辑复杂度高 | 中 | 高 | 重点测试，确保克隆目标在正确的 project+env 下 |

---

## 七、验收标准（AC）

### AC-APP-01：新路由创建 App
- [ ] 请求携带 `project_id=1, env_id=2` 创建 App
- [ ] DB 中 `applications.project_id=1, environment_id=2`
- [ ] 后续 GetApp 用相同维度能查到

### AC-APP-02：新路由查询 App
- [ ] 请求携带正确 `project_id+env_id+app_id` → 返回 App 详情
- [ ] 请求携带不匹配的 `project_id` → 返回 not found（中间件拦截）
- [ ] 请求携带匹配 `project_id` 但不匹配 `env_id` → 返回 not found

### AC-APP-03：旧路由 Fallback
- [ ] 请求不携带 `project_id/env_id`（或传 0）
- [ ] 自动使用默认值（当前硬编码 7,7，后续改为配置值）
- [ ] 创建/查询/更新/删除均正常工作
- [ ] DB 记录中 `project_id=默认值, env_id=默认值`

### AC-APP-04：跨项目隔离
- [ ] 项目 A 的用户无法通过任何路由访问项目 B 的 App
- [ ] ListApps 只返回当前项目+环境下的 App

### AC-APP-05：冗余代码清理
- [ ] data-service 中 28 处 `// 校验 app 归属` 代码块全部删除
- [ ] `go build ./...` 编译通过
- [ ] `go test ./cmd/data-service/...` 单包测试通过

### AC-APP-06：中间件非硬编码
- [ ] `checkOrCreateDefaultProjectEnv` 不再有字面量 `7`
- [ ] 默认值来自配置文件或 RPC 查询
